{
    "name": "root",
    "private": true,
    "scripts": {
        "publish:canary": "lerna publish --no-commit-hooks --canary --pre-dist-tag alpha --yes"
    },
    "devDependencies": {
        "lerna": "^3.15.0"
    }
}