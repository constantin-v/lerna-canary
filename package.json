{
    "name": "root",
    "private": true,
    "scripts": {
        "prepublish": "node test.js",
        "publish": "lerna publish --no-commit-hooks --yes --loglevel=verbose",
        "publish:canary": "lerna publish --no-commit-hooks --canary --pre-dist-tag alpha --yes --loglevel=verbose"
    },
    "devDependencies": {
        "lerna": "^3.20.2"
    }
}